# GitHub Pages

O feed de update do VSCodium 1.126:

`{updateUrl}/stable/win32/x64/user/latest.json`

Esta pasta `docs/` é a raiz do site `https://jarvis-advpl.github.io/jarvis-ide/`.
Habilite Pages: Settings → Pages → Deploy from branch `main` → folder `/docs`.
